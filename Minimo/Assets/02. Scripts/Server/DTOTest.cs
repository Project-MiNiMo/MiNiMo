using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using MinimoShared;

public class DTOTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var playerDTO = new PlayerDTO();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
